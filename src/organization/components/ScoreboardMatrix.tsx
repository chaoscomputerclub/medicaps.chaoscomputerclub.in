import type { ContestProblem, ScoreboardEntry } from "../data/types";
import { cn } from "@/lib/utils";
import { formatPenalty } from "./ui";
export function ScoreboardMatrix({entries,problems,compact=false}:{entries:ScoreboardEntry[];problems:ContestProblem[];compact?:boolean}){if(!entries||entries.length===0)return <div className="text-center py-10 text-[#666] font-mono text-xs border border-dashed border-[#222]">No standings or telemetry recorded yet.</div>;return <div className="table-scroll"><table className="scoreboard"><thead><tr><th>#</th><th>Contestant</th>{!compact&&<th>Dept.</th>}{problems.map(p=><th key={p.index} title={p.title}>{p.index}</th>)}<th>Solved</th><th>Penalty</th><th>Δ</th></tr></thead><tbody>{entries.map(row=><tr key={row.handle} className={cn(row.is_you&&"is-you")}><td><strong>{row.rank}</strong></td><td><div className="competitor"><strong>{row.handle}</strong><span>{row.full_name}</span></div></td>{!compact&&<td><span className="mono-tag">{row.department} · {row.batch.slice(2)}</span></td>}{problems.map((p,i)=>{const cell=row.problems[i];return <td key={p.index}><span className={cn("problem-cell",cell?.solved?"solved":"missed",cell?.first_ac&&"first-ac")}>{cell?.solved?formatPenalty(cell.solve_seconds??0).slice(3):cell?.wrong_attempts?`−${cell.wrong_attempts}`:"·"}</span></td>})}<td className="score-value">{row.solved}</td><td className="mono-value">{formatPenalty(row.penalty_seconds)}</td><td className={cn("delta",row.rating_delta>=0?"positive":"negative")}>{row.rating_delta>0?"+":""}{row.rating_delta}</td></tr>)}</tbody></table></div>}
