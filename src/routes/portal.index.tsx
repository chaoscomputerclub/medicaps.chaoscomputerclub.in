import { createFileRoute, Link } from "@tanstack/react-router";
import { useSuspenseQuery } from "@tanstack/react-query";
import { ArrowRight, CalendarClock, MapPin, Radio, ShieldCheck } from "lucide-react";
import { Button } from "@/components/ui/button";
import { RatingChart } from "@/organization/components/RatingChart";
import { ScoreboardMatrix } from "@/organization/components/ScoreboardMatrix";
import { Metric, SectionHeader, StatusDot, TierBadge, formatContestDate } from "@/organization/components/ui";
import { portalQueries } from "@/organization/data/queries";
const queries=[portalQueries.member(),portalQueries.contests(),portalQueries.announcements(),portalQueries.ratingHistory()] as const;
export const Route=createFileRoute("/portal/")({head:()=>({meta:[{title:"Offline Contest Operations — CCC Medi-Caps"},{name:"description",content:"Live operations, ratings, verified results and campus contest intelligence for CCC Medi-Caps."},{property:"og:title",content:"CCC Medi-Caps Offline Contest Operations"},{property:"og:description",content:"Proctored campus contests, live standings and verifiable results."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),loader:({context})=>Promise.all([context.queryClient.ensureQueryData(queries[0]),context.queryClient.ensureQueryData(queries[1]),context.queryClient.ensureQueryData(queries[2]),context.queryClient.ensureQueryData(queries[3])]),component:Dashboard});
function Dashboard(){const {data:member}=useSuspenseQuery(queries[0]);const {data:contests}=useSuspenseQuery(queries[1]);const {data:feed}=useSuspenseQuery(queries[2]);const {data:history}=useSuspenseQuery(queries[3]);const live=contests.find(c=>c.status==="live")??contests[0];const next=contests.find(c=>c.status==="upcoming");if(!live)return null;return <div className="page-wrap"><header className="page-header"><div><p className="kicker">Member operations console</p><h1>Good morning, {member.full_name.split(" ")[0]}.</h1><p>Your competitive record is only written inside a verified Medi-Caps contest room.</p></div><div className="member-rating"><TierBadge>{member.tier}</TierBadge><strong>{member.rating}</strong><span>UNIVERSITY RANK #{member.university_rank}</span></div></header><section className="live-command"><div className="live-copy"><StatusDot status="live"/><p className="kicker">Now running · {live.season}</p><h2>{live.title}</h2><p>{live.summary}</p><div className="event-facts"><span><MapPin size={14}/>{live.venue}</span><span><Radio size={14}/>{live.environment}</span></div><div className="button-row"><Button asChild><Link to="/portal/contests/$contestSlug" params={{contestSlug:live.slug}}>Open contest room <ArrowRight/></Link></Button><Button variant="outline" asChild><Link to="/portal/profile">View campus pass</Link></Button></div></div><div className="live-stats"><Metric label="Registered" value={`${live.registered_count}/${live.seat_capacity}`} detail="Physical seats"/><Metric label="Problems" value={live.problem_count} detail="Sealed set"/><Metric label="Your room" value="Lab 402" detail="Seat C-17"/><Metric label="Freeze" value="12:30" detail="IST today"/></div></section><section className="metrics-grid"><Metric label="Current rating" value={member.rating} detail={`Peak ${member.peak_rating}`}/><Metric label="University rank" value={`#${member.university_rank}`} detail={`of ${member.active_members} active`}/><Metric label="Offline battles" value={member.attendance_total} detail={`${member.attendance_count} verified`}/><Metric label="Podium finishes" value={member.podiums} detail={`${member.streak} battle streak`}/></section><section className="content-grid"><div className="panel wide"><SectionHeader kicker="Elo trajectory" title="Rating progression"/><RatingChart data={history}/></div><div className="panel"><SectionHeader kicker="Operations feed" title="Chapter signals"/><div className="feed-list">{feed.map(item=><article key={item.id}><span>{item.kind.replace("_"," ")}</span><h3>{item.title}</h3><p>{item.summary}</p><time>{formatContestDate(item.published_at)}</time></article>)}</div></div></section><section className="panel standings-panel"><SectionHeader kicker="Live telemetry" title="Top of the room" action={<Link to="/portal/leaderboard" className="text-link">Full leaderboard <ArrowRight size={14}/></Link>}/><ScoreboardMatrix entries={live.standings.slice(0,5)} problems={live.problems} compact/></section>{next&&<section className="next-operation"><div><CalendarClock/><span><small>Next operation</small><strong>{next.title}</strong><em>{formatContestDate(next.starts_at)} · {next.venue}</em></span></div><Link to="/portal/contests/$contestSlug" params={{contestSlug:next.slug}}>Review brief <ArrowRight/></Link></section>}<footer className="trust-footer"><ShieldCheck/><p><strong>Proof before prestige.</strong> Every score joins the public record only after attendance, workstation, and proctor logs reconcile.</p></footer></div>}
