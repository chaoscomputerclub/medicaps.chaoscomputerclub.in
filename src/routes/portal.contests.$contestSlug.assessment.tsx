import {createFileRoute,Link,notFound,useNavigate} from "@tanstack/react-router";
import {useSuspenseQuery} from "@tanstack/react-query";
import {useState} from "react";
import {AlertTriangle,ArrowLeft,CheckCircle2,Clock3,Flag,Hourglass,Send} from "lucide-react";
import {Button} from "@/components/ui/button";
import {Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle,DialogTrigger} from "@/components/ui/dialog";
import {cn} from "@/lib/utils";
import {StatePanel} from "@/organization/components/ContestSystemUI";
import {contestSystemQueries} from "@/organization/data/contest-queries";
import {reviewState} from "@/organization/data/contest-system";
export const Route=createFileRoute("/portal/contests/$contestSlug/assessment")({validateSearch:(s:Record<string,unknown>)=>({state:reviewState(s["state"])}),loader:async({context,params})=>{const c=await context.queryClient.ensureQueryData(contestSystemQueries.contest(params.contestSlug));if(!c)throw notFound();return c},head:()=>({meta:[{title:"Online Assessment — CCC Medi-Caps"},{name:"description",content:"Complete Round 1 of the CCC Medi-Caps two-stage contest."},{property:"og:title",content:"Online Assessment — CCC Medi-Caps"},{property:"og:description",content:"Timed Round 1 assessment workspace."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Assessment});
function Assessment(){const {contestSlug}=Route.useParams();const {state}=Route.useSearch();const navigate=useNavigate({from:Route.fullPath});const {data:c}=useSuspenseQuery(contestSystemQueries.contest(contestSlug));const [active,setActive]=useState(1);const [answers,setAnswers]=useState<Record<string,number>>({});if(!c)return null;if(state==="waiting"||state==="default")return <div className="page-wrap narrow"><StatePanel icon={<Hourglass/>} kicker="Round 1 · Waiting room" title="Your assessment opens soon"><p>Your registration is confirmed. Keep this page available; the start control will become active when the assessment window opens.</p><div className="countdown-grid"><span><strong>02</strong>days</span><span><strong>04</strong>hours</span><span><strong>18</strong>minutes</span></div><dl className="waiting-facts"><div><dt>Duration</dt><dd>{c.assessment.duration_minutes} minutes</dd></div><div><dt>Problems</dt><dd>{c.assessment.problems.length}</dd></div><div><dt>Window closes</dt><dd>19:30 IST</dd></div></dl></StatePanel></div>;if(state==="submitted")return <div className="page-wrap narrow"><StatePanel icon={<CheckCircle2/>} kicker="Round 1 · Submitted" title="Assessment received"><p>Your final response set was submitted at 19:12 IST. Results remain sealed until verification is complete.</p></StatePanel></div>;const p=c.assessment.problems[active]??c.assessment.problems[0];if(!p)return null;return <div className="assessment-shell"><header className="assessment-bar"><Link to="/portal/contests/$contestSlug" params={{contestSlug}} search={{state:"default"}} aria-label="Return to contest"><ArrowLeft/></Link><div><p className="kicker">Round 1 · Online assessment</p><strong>{c.title}</strong></div><div className="assessment-clock"><Clock3/><span>00:54:07</span><small>REMAINING</small></div><Dialog><DialogTrigger asChild><Button><Send/>Finish assessment</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Submit your final responses?</DialogTitle><DialogDescription>You can review unanswered and flagged problems before submitting. Once submitted, this assessment cannot be reopened.</DialogDescription></DialogHeader><div className="submission-check"><span>Answered <strong>{Object.keys(answers).length}/{c.assessment.problems.length}</strong></span><span>Flagged <strong>1</strong></span></div><DialogFooter><Button variant="outline">Keep reviewing</Button><Button onClick={()=>void navigate({search:{state:"submitted"}})}>Submit assessment</Button></DialogFooter></DialogContent></Dialog></header><div className="assessment-progress"><span style={{width:`${Object.keys(answers).length/c.assessment.problems.length*100}%`}}/></div><main className="assessment-workspace"><aside className="assessment-nav"><div><strong>Problem set</strong><span>{Object.keys(answers).length} of {c.assessment.problems.length} answered</span></div>{c.assessment.problems.map((x,i)=><Button key={x.id} variant="ghost" className={cn(i===active&&"active",answers[x.id]!==undefined&&"answered")} onClick={()=>setActive(i)}><span>{x.index}</span><div><strong>{x.title}</strong><small>{x.points} points</small></div>{x.status==="flagged"&&<Flag/>}</Button>)}</aside><section className="assessment-question"><div className="question-meta"><span>Problem {p.index} of {c.assessment.problems.length}</span><span>{p.category}</span><strong>{p.points} points</strong></div><h1>{p.title}</h1><p>{p.prompt}</p><fieldset><legend>Select one answer</legend>{p.options.map((x,i)=><label className={answers[p.id]===i?"selected":""} key={x}><input type="radio" name={p.id} checked={answers[p.id]===i} onChange={()=>setAnswers(a=>({...a,[p.id]:i}))}/><span>{String.fromCharCode(65+i)}</span><strong>{x}</strong></label>)}</fieldset><div className="assessment-actions"><Button variant="outline"><Flag/>Flag for review</Button><Button onClick={()=>setActive(Math.min(active+1,c.assessment.problems.length-1))}>Save and continue</Button></div></section><aside className="assessment-rules"><AlertTriangle/><strong>Assessment integrity</strong><p>Your timer continues if you leave this page. Submit before exiting the assessment.</p><dl><div><dt>Autosave</dt><dd>On</dd></div><div><dt>Last saved</dt><dd>Just now</dd></div></dl></aside></main></div>}
