import { ProblemDetailSkeleton } from '@/organization/components/skeletons';
import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { ArrowLeft, Ban, BookOpenText, CheckCircle2 } from "lucide-react";
import { portalQueries } from "@/organization/data/queries";
export const Route=createFileRoute("/portal/problems/$problemSlug")({loader:async({context,params})=>{const [slug,index]=params.problemSlug.split("--");const contest=await context.queryClient.ensureQueryData(portalQueries.contest(slug??""));const problem=contest?.status==="finished"?contest.problems.find((p: any)=>p.index.toLowerCase()===index):undefined;if(!contest||!problem)throw notFound();return{contest,problem}},head:({loaderData})=>({meta:[{title:loaderData?`${loaderData.problem.title} — CCC Archive`:"Problem unavailable"},{name:"description",content:loaderData?.problem.editorial??"Archived problem unavailable."},{property:"og:title",content:loaderData?.problem.title??"Problem unavailable"},{property:"og:description",content:loaderData?.problem.editorial??"Archived problem unavailable."},{property:"og:type",content:"article"},{name:"twitter:card",content:"summary_large_image"}]}),notFoundComponent:()=> <div className="page-wrap"><h1>Archived problem not found.</h1></div>,pendingComponent:ProblemDetailSkeleton,component:Problem});
function Problem(){const {contest,problem}=Route.useLoaderData();return <div className="page-wrap narrow"><Link to="/portal/problems" className="back-link"><ArrowLeft/> Problem archive</Link><header className="problem-header"><span>{problem.index}</span><div><p className="kicker">{contest.title} · {problem.points} points</p><h1>{problem.title}</h1><p>{problem.topic} · {problem.solved_count} verified solves</p></div></header><div className="readonly-notice"><Ban/><div><strong>No online submission surface</strong><p>This statement is preserved for study. Official attempts were accepted only from assigned campus workstations during the contest window.</p></div></div><article className="editorial-body"><p className="kicker">Official editorial</p><h2>Post-contest analysis</h2><p>{problem.editorial}</p><h3>Core observation</h3><p>Model the invariant before choosing a data structure. The intended solution maintains a monotonic decision boundary and proves every discarded state cannot improve the final answer.</p><pre><code>{`complexity: O(n log n)\nspace: O(n)\nverdict source: sealed judge replay`}</code></pre><h3><CheckCircle2/> Verification note</h3><p>The solve count and first-accept timing were reconciled against workstation logs before publication.</p></article></div>}
